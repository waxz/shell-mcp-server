
# ALLOWED_BASH= ["/bin/bash","/bin/sh"]
# ALLOWED_POWERSHELL= ["powershell.exe","pwsh.exe"]
# ALLOWED_CMD= ["cmd.exe"]
ALLOWED_DIRECTORIES = ['.']

SAFETY_MODE = "strict"
# SAFETY_MODE = "relax"

TRUSTED_COMMANDS.uv_install = { command = "uv pip install .", shell = "powershell", cwd = "." }
